@model PostalManagementMVC.Entities.Location

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@* forma ne razor view per te konfirmuar dhe fshire nje vendodhje *@
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Location</h4>
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <dl class="row">
        <dt class = "col-sm-10">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-10">
            @Html.DisplayNameFor(model => model.IsPostOffice)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsPostOffice)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        @if (ViewBag.HasActiveRelatedRecords != true)
        {
            <input type="submit" value="Delete" class="btn btn-danger" />
            <span> | </span>
        }
        <a asp-action="Index" asp-route-officeId="@Model?.PostOfficeInChargeId" class="btn btn-primary">Back to List</a>
    </form>
</div>
